import { CadastroCredentials } from './cadastro-credentials';

describe('CadastroCredentials', () => {
  it('should create an instance', () => {
    expect(new CadastroCredentials()).toBeTruthy();
  });
});
